# Normalizuj końce linii (repo trzyma LF, build w CI działa na Linux).
* text=auto eol=lf

# Pliki Windows zostają z CRLF.
*.bat text eol=crlf

# Pliki binarne — bez normalizacji.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
